version: '3.5'

services:
  nginx:
    container_name: nginx
    image: nginx:1.0
    volumes:
      - wordpress_vol:/var/www/html
    build: requirements/nginx
    ports:
      - "5455:5455"
    restart: on-failure

volumes:
  wordpress_vol:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/home/mbocquel/Documents/webserv/docker_nginx/sites'